pipeline {
    agent any

    tools {
        maven 'maven 3.8.6'
    }

    stages {
        stage('Clean') {
            steps {
                sh "mvn clean install -pl /order-service"
            }
        }
       stage('Test') {
            steps {
                sh "mvn test -pl /order-service"
            }
       }
       stage('Package') {
           steps {
               sh "mvn package -pl /order-service"
           }
      }
   }
}