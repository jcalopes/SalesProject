pipeline {
    agent any

    tools {
        maven 'maven 3.8.6'
    }

    stages {
        stage('Clean') {
            steps {
                sh "mvn clean install -pl /discovery-service"
            }
        }
       stage('Test') {
            steps {
                sh "mvn test -pl /discovery-service"
            }
       }
       stage('Package') {
           steps {
               sh "mvn package -pl /discovery-service"
           }
      }
   }
}