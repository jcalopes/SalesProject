pipeline {
    agent any

    tools {
        maven 'maven 3.8.6'
    }

    stages {
        stage('Clean') {
            steps {
                sh "mvn clean install -pl /inventory-service"
            }
        }
       stage('Test') {
            steps {
                sh "mvn test -pl /inventory-service"
            }
       }
       stage('Package') {
           steps {
               sh "mvn package -pl /inventory-service"
           }
      }
   }
}